// Minimal forward include guard to silence editors;
#ifndef STB_IMAGE_FORWARD_DECL_ONLY
#define STB_IMAGE_FORWARD_DECL_ONLY

// The full stb_image.h content is pulled in by texture.h via #include "stb_image.h".
#include <stdint.h>

#endif 
